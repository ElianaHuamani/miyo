export interface Step {
    title: string;       // Nombre del paso (ej. "Lección 1")
    completed: boolean;  // Si el paso ya está completado
    link: string;        // Enlace al contenido del paso
  }
  